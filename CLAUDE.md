# 이 프로젝트에 대해

`소방공고알리미`는 잡코리아/원티드/사람인에서 "소방" 관련 채용공고를 매일 자동으로 찾아서
카카오톡 "나에게 보내기"로 전송해주는 프로그램입니다.

- `config.json`: 카카오 API 키, 시크릿, 리프레시 토큰, 검색 키워드 저장
- `seen.json`: 이미 보낸 공고 기록 (중복 전송 방지)
- `alert.js`: 실제 크롤링 + 카카오톡 전송을 수행하는 메인 스크립트 (Node.js로 작성)
- 매일 낮 12시에 자동 실행되도록 예약 설정 예정

## 중요 규칙: 기술 설명은 항상 LoL(리그오브레전드) 비유로 할 것

사용자는 개발/IT 용어(HTML, 크롤링, 파이썬, API 등)에 익숙하지 않습니다.
기술적인 작업을 설명할 때는 **항상 리그오브레전드 비유를 사용해서** 쉽게 설명하세요.
"크롤링", "파싱", "API", "토큰" 같은 용어를 그냥 쓰지 말고, 아래처럼 롤 상황에 빗대어 풀어서 설명할 것.

### 비유 대응표 (예시)
- **크롤링(crawling)** = 정글러가 맵을 돌면서 몹(채용공고)이 어디 있는지 스캔하고 다니는 것
- **HTML(웹페이지 구조)** = 미니맵/전장 정보 — 그 안에서 우리가 원하는 정보(오브젝트 위치)를 찾아내는 것
- **API(정식으로 제공하는 데이터 통로)** = 상점(스토어)에서 바로 아이템 정보를 받아오는 것 (와드 안 박아도 바로 정보를 줌)
- **크롤링(HTML 직접 읽기)** = 상점 정보가 따로 없어서, 직접 와드/정찰로 눈으로 훑어서 정보를 캐내는 것
- **파이썬/Node.js 같은 프로그래밍 언어** = 이번 판에 선택한 챔피언 (이번엔 파이썬 대신 이미 우리 팀에 있던 "Node.js"라는 챔피언을 픽함)
- **토큰(access token)** = 그날그날 쓰는 일일 출입증 — 시간 지나면 만료돼서 다시 받아야 함
- **리프레시 토큰(refresh token)** = 그 출입증을 매번 새로 안 받고 자동으로 갱신해주는 시즌권 같은 것
- **스크립트 실행** = 정해진 루틴대로 자동으로 게임을 돌리는 매크로(자동 파밍) 같은 것
- **중복 방지(seen.json)** = 이미 잡은 몹은 다시 안 잡도록 체크하는 킬로그 같은 것

### 예시 설명 (실제 이번 작업을 다시 설명한다면)

> 아까 제가 "크롤링 스크립트를 작성한다"고 했던 거, 롤로 치면 이런 거예요.
>
> 잡코리아/원티드/사람인 이 세 사이트를 각각 "적 정글" 이라고 생각해보세요. 원티드는 친절하게 "상점(API)"이 따로 있어서, 그냥 상점 가서 "소방 관련 아이템 주세요" 하면 필요한 정보(공고 제목, 회사명, 링크)를 바로 정리해서 줘요.
>
> 근데 잡코리아, 사람인은 그런 상점이 없어요. 그래서 대신 정글러처럼 직접 맵(웹페이지의 HTML)을 돌면서, 눈에 보이는 정보 중에 "소방"이라는 글자가 들어간 몹(공고)만 골라서 잡아오는 거예요 — 이게 "크롤링"이에요.
>
> 이 정찰 루틴을 자동으로 돌게 만든 게 `alert.js` 스크립트고, 이걸 이번엔 "Node.js"라는 챔피언으로 짰어요 (원래 쓰려던 파이썬 챔피언은 이 컴퓨터에 설치가 안 돼있어서, 이미 우리 팀에 있던 Node.js로 대신 픽한 거예요).
>
> 그리고 카카오톡으로 메시지 보내는 권한은 "출입증(토큰)"이 있어야 하는데, 이 출입증은 매일 새로 받아야 하는 게 귀찮으니까, "리프레시 토큰"이라는 시즌권을 하나 받아뒀어요. 이걸로 매번 자동으로 새 출입증을 발급받게 만들어놨습니다.
>
> 마지막으로, 이미 보낸 공고를 또 보내면 스팸이 되니까, `seen.json`이라는 파일에 "이미 잡은 몹 리스트(킬로그)"를 기록해두고, 다음번엔 그 리스트에 없는 새 몹만 잡아서 보내줘요.
